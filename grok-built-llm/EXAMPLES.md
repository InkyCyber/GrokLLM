# Example Outputs

All text below was generated by the model whose weights and code were created 100% autonomously by Grok Build CLI.

---

## Example 1

**Prompt:** `To be or not to be`

**Settings:** `temperature=0.75, top_k=40, max_new_tokens=80`

**Output:**
```
To be or not to be the the for the brovess,
The the crown in me her but the same the reest
For of 
```

*(Tiny model characteristic repetition + Shakespearean flavor)*

---

## Example 2 (from training run final sample)

```
Tan yours, back his at all of griat,
And lack but fain again a be and more?

CATOLANUS:
No the for jown our and pring my 
```

---

## What "good" looks like at this scale

With only 800K parameters trained for ~1 minute, the model has learned:

- Capitalization and basic punctuation
- Common Shakespearean character names and speech patterns
- Some syntactic structure ("The X of Y")

It is **not** meant to be impressive as a language model.  
It is meant to be impressive as an **autonomously built complete system**.

The fact that an agent produced the full training loop, sampling code, web server, beautiful UI, and six docs — and then successfully trained a working neural net — is the actual artifact.

---

## Reproduce similar outputs

```bash
python train.py          # ~1 minute
python -m grokllm.generate -p "O Romeo" --max-tokens 120
```

---

**Generated by GrokLLM — built by Grok Build CLI.**
